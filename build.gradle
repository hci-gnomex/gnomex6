import org.apache.tools.ant.filters.FixCrLfFilter;

buildscript {
    repositories {
        maven {
            credentials {
                username = 'u0974549'
                password = 'AP6b32YbsxUzayr5KPBeNziaTrVispH4d4ejJ9'
            }
            url 'https://ri-artifact-new.hci.utah.edu/artifactory/libs-release'
        }
        maven { url 'https://repo.spring.io/plugins-snapshot' }
    }
    dependencies {
        classpath 'org.springframework.build.gradle:propdeps-plugin:0.0.8-SNAPSHOT'
        classpath 'org.jfrog.buildinfo:build-info-extractor-gradle:3.0.1'
        classpath 'hci:hci-conventions:2.0.0'
        classpath 'com.amazonaws:aws-java-sdk-bom:1.12.411'

    }
}


plugins {
    id 'war'
    id 'idea'
    id 'maven-publish'
}

apply plugin: 'maven-publish'
apply from: 'common.gradle'

repositories {
    maven {
        credentials {
            username = 'u0974549'
            password = 'AP6b32YbsxUzayr5KPBeNziaTrVispH4d4ejJ9'
        }
        url 'https://ri-artifact-new.hci.utah.edu/artifactory/libs-release'
    }
    maven { url 'https://repo.spring.io/plugins-snapshot' }

    mavenCentral()
}

dependencies {

    implementation platform('com.amazonaws:aws-java-sdk-bom:1.12.132')
    implementation 'com.amazonaws:aws-java-sdk-s3'


    runtimeOnly group: 'hci', name: 'hci-servlet-filter-nocache', version: '1.+'
    runtimeOnly group: 'hci', name: 'hci-servlet-filter-cors', version: '1.+'
    runtimeOnly group: 'hci', name: 'hci-servlet-filter-pushstate', version: '1.+'

    implementation group: 'hci', name: 'auth-services', version: '4.+'
    implementation group: 'hci', name: 'auth-persistence', version: '3.1+'
    implementation group: 'hci', name: 'hci-bean-validation', version: '+'

    implementation group: 'servlets.com', name: 'cos', version: '05Nov2002'
    implementation group: 'org.apache.commons', name: 'commons-compress', version: '1.1'
    implementation group: 'org.ostermiller', name: 'utils', version: '1.07.00'
    implementation group: 'net.sf.json-lib', name: 'json-lib', version: '2.4', classifier: 'jdk15'
    implementation group: 'com.itextpdf', name: 'itextpdf', version: '5.5.9'
    implementation group: 'org.freemarker', name: 'freemarker', version: '2.3.20'
    implementation group: 'commons-validator', name: 'commons-validator', version: '1.4.0'
    implementation group: 'org.apache.lucene', name: 'lucene-core', version: '2.9.4'
    implementation group: 'commons-codec', name: 'commons-codec', version: '1.10'
    implementation group: 'commons-io', name: 'commons-io', version: '2.4'
    implementation group: 'commons-lang', name: 'commons-lang', version: '2.6'
    implementation group: 'jdom', name: 'jdom', version: '1.0b8'
    implementation group: 'com.fasterxml.jackson.core', name: 'jackson-core', version: '2.9.7'
    implementation group: 'com.github.fracpete', name: 'rsync4j-all', version: '3.1.2-17'

    implementation fileTree(dir: 'libEnv', include: 'jakarta.mail-api-2.1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'jakarta.activation-api-2.1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'angus-mail-2.0.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'angus-activation-2.0.2.jar')

    implementation group: 'jline', name: 'jline', version: '0.9.94'
    implementation group: 'xom', name: 'xom', version: '1.2.5'

    //lib & libEnv
    implementation fileTree(dir: 'libEnv', include: 'hci-hibernate5-utils-1.0.jar')
    implementation fileTree(dir: 'lib', include: 'biojava.jar')
    implementation fileTree(dir: 'lib', include: 'metrixClient-1.4.jar')
//    implementation fileTree(dir: 'libEnv', include: 'hci_framework.jar')
    implementation fileTree(dir: 'libEnv', include: 'hci_utils.jar')
    implementation fileTree(dir: 'libEnv', include: 'HCIReport.jar')
    implementation fileTree(dir: 'libEnv', include: 'jdom-2.0.6.jar')
    implementation fileTree(dir: 'libEnv', include: 'jaxen-1.1.6.jar')
    implementation fileTree(dir: 'libEnv', include: 'poi-3.10-FINAL.jar')
    implementation fileTree(dir: 'libEnv', include: 'javax.json-api-jdk8-1.1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'javax.json-jdk8-1.1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'hci-servlet-filter-xsrf-1.1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'rsync4j-3.1.2-8.jar')
    implementation fileTree(dir: 'libEnv', include: 'DuoWeb-1.3.jar')
    implementation fileTree(dir: 'libEnv', include: 'processoutput4j-0.0.7.jar')
    implementation fileTree(dir: 'libEnv', include: 'argparse4j-0.6.0.jar')
    implementation fileTree(dir: 'libEnv', include: 'jdk-misc-3.Final.jar')
    implementation fileTree(dir: 'libEnv', include: 'jaxb-api-2.3.1.jar')
    implementation fileTree(dir: 'libEnv', include: 'jaxb-core-2.3.0.1.jar')
    implementation fileTree(dir: 'libEnv', include: 'jaxb-impl-2.3.1.jar')
    implementation fileTree(dir: 'libEnv', include: 'ant-1.10.13.jar')
    implementation fileTree(dir: 'libEnv', include: 'ant-launcher-1.10.13.jar')

//    providedCompile group: 'org.apache.logging.log4j', name: 'log4j-api', version: '2.3.2'
//    providedCompile group: 'org.apache.logging.log4j', name: 'log4j-core', version: '2.3.2'

//    implementation fileTree(dir: 'libEnv', include: 'aws-java-sdk-core-1.122.411.jar')
//    implementation fileTree(dir: 'libEnv', include: 'aws-java-sdk-s3-1.122.411.jar')

    //Realm stuff
//    providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version: '3.0.1'
//    providedCompile group: 'log4j', name: 'log4j', version: '1.2.12'
    providedCompile group: 'org.apache.tomcat', name: 'tomcat-jdbc', version: '9.0.0.M10'
    providedCompile group: 'org.apache.tomcat', name: 'tomcat-catalina', version: '9.0.10'

    //Subprojects
    providedCompile project(':gnomex_crypt')
    providedCompile project(':gnomex_tomcat_support')

    //Tomcat
    implementation group: 'org.hibernate', name: 'hibernate-core', version: '5.2.2.Final'
//    implementation group: 'org.hibernate', name: 'hibernate-entitymanager', version: '5.0.10.Final'
    implementation group: 'org.slf4j', name: 'slf4j-api', version: '1.7.+'
//    implementation group: 'commons-codec', name: 'commons-codec', version: '1.10'
//    implementation group: 'commons-io', name: 'commons-io', version: '2.4'
//    implementation group: 'commons-lang', name: 'commons-lang', version: '2.6'

    // JAX-RS Related
    implementation group: 'org.jboss.resteasy', name: 'resteasy-jaxrs', version: '3.0.19.Final'
    implementation group: 'org.jboss.resteasy', name: 'resteasy-jackson2-provider', version: '3.0.19.Final'
    implementation group: 'org.jboss.resteasy', name: 'resteasy-client', version: '3.0.19.Final'
    implementation group: 'org.jboss.resteasy', name: 'resteasy-servlet-initializer', version: '3.0.19.Final'
    implementation group: 'org.jboss.resteasy', name: 'resteasy-cdi', version: '3.0.19.Final'

    // CDI Related
    providedCompile group: 'javax.ejb', name: 'javax.ejb-api', version: '3.2'
    providedCompile group: 'javax.enterprise', name: 'cdi-api', version: '1.2'
    runtimeOnly group: 'org.jboss.weld.servlet', name: 'weld-servlet', version: '2.4.1.Final'

    // https://mvnrepository.com/artifact/ch.qos.reload4j/reload4j
    implementation group:  'ch.qos.reload4j', name: 'reload4j', version: '1.2.26'
}


// Main build tasks are named starting with "gnomex"
task gnomexWindowsWDS(type: GradleBuild) {
    tasks = ['adjustShiroForWindows', 'buildWar', 'copyAngularQuick', 'copyWar']
}

task gnomexWindowsTomcat(type: GradleBuild) {
    tasks = ['adjustShiroForWindows', 'buildWar', 'copyAngular', 'copyWar']
}

task gnomexLinuxTomcat(type: GradleBuild) {
    tasks = ['adjustShiroForLinux', 'buildWar', 'copyAngular', 'copyWar']
}

task gnomexOpensource(type: GradleBuild) {
    tasks = ['adjustShiroForLinux', 'buildWarOpensource', 'copyAngular', 'copyWar', 'createOpenSourceZip']
}

task adjustShiroForLinux() {
    doLast {
        copy {
            from "src/main/webapp/WEB-INF"
            include "shiro.ini"
            filter { line -> line.replace(" C:/properties", " /properties")}
            into "src/main/webapp/WEB-INF/temp"
        }
        copy {
            from "src/main/webapp/WEB-INF/temp"
            include "shiro.ini"
            into "src/main/webapp/WEB-INF"
        }
        delete {
            delete "src/main/webapp/WEB-INF/temp"
        }
    }
}

task adjustShiroForWindows() {
    doLast {
        copy {
            from "src/main/webapp/WEB-INF"
            include "shiro.ini"
            filter { line -> line.replace(" /properties", " C:/properties")}
            into "src/main/webapp/WEB-INF/temp"
        }
        copy {
            from "src/main/webapp/WEB-INF/temp"
            include "shiro.ini"
            into "src/main/webapp/WEB-INF"
        }
        delete {
            delete "src/main/webapp/WEB-INF/temp"
        }
    }
}

task buildWar(type: GradleBuild) {
    mustRunAfter(adjustShiroForWindows, adjustShiroForLinux)
    tasks = ['cleanTomcat', 'makeSQLServer', 'jar', 'clientJar', 'copyScripts', 'copyTomcatJars', 'war']
}

task buildWarOpensource(type: GradleBuild) {
    mustRunAfter(adjustShiroForWindows, adjustShiroForLinux)
    tasks = ['clean', 'cleanTomcat', 'makeMySQL', 'jar', 'clientJar', 'copyScripts', 'copyTomcatJars', 'opensourceWar']
}

task cleanTomcat(type: Delete) {
//    delete "${gnomexServerHome}/${gnomexDeployDir}/gnomex"
}

static void replaceString(String fileName, String fromString, String toString) {
    File file = new File(fileName)
    String fileText = file.text
    fileText = fileText.replace(fromString, toString)
    new File(fileName).write(fileText)
}

task makeMySQL() {
    replaceString("${rescDir}/hibernate.tomcat.cfg.xml", "SQLServerDialect", "MySQLDialect")
}

task makeSQLServer() {
    replaceString("${rescDir}/hibernate.tomcat.cfg.xml", "MySQLDialect", "SQLServerDialect")
}

jar {
    archiveFileName = jarName
    manifest {
        attributes(
                'Class-Path': 'log4j.jar'
        )
        attributes([
                'Built-By'              : 'u0974549',
                'Implementation-Version': "6.26.12 (07/06/2025)"
        ], 'gnomex')
    }
}

task clientJar(type: Jar) {
    from buildDir.getAbsolutePath() + "/classes/main/"
    include "hci/gnomex/httpclient/*.class"
    archiveFileName = clientJarName

    manifest {
        attributes([
                'Built-By'              : 'u0974549',
                'Implementation-Version': '6.26.12'
        ], 'gnomexClient')
    }
}

task copyScripts(type: Copy) {
    from 'scripts'
    include '*.*'
    into buildDir.getAbsolutePath() + "/scripts"
    filter(FixCrLfFilter)
}

task copyTomcatJars(type: Copy) {
    dependsOn ':gnomex_tomcat_support:jar', ':gnomex_crypt:jar'
    from 'gnomex_tomcat_support/build/libs'
    from 'gnomex_crypt/build/libs'
    into "${gnomexServerHome}/lib"
}

war {
    archiveFileName = warNameTemp
    classpath = classpath - sourceSets.main.output

    duplicatesStrategy = DuplicatesStrategy.EXCLUDE

    manifest {
        attributes(
                'Built-By': 'u0974549',
                'Implementation-Version': "6.26.12 (07/06/2025)",
                'Dependencies': 'org.dom4j, org.apache.log4j'
        )
    }

    rootSpec.exclude("**/tomcat-annotations-api-7.0.68.jar")
    rootSpec.exclude("**/tomcat-api-7.0.68.jar")
    rootSpec.exclude("**/tomcat-catalina-7.0.68.jar")
    rootSpec.exclude("**/tomcat-servlet-api-7.0.68.jar")
    rootSpec.exclude("**/tomcat-util-7.0.68.jar")

    from(buildDir.getAbsolutePath() + "/libs") {
        include "${jarName}"
        include "${clientJarName}"
        into 'WEB-INF/lib'
    }

    from("$projectDir/META-INF") {
        include "context.xml"
        into 'META-INF'
    }

    from("$buildDir/scripts") {
        into "scripts"
    }

    from(rescDir) {
        include '*.*'
        into 'WEB-INF/classes'
    }

    from("${rescDir}/hci/gnomex") {
        include 'Dictionaries.xml'
        into 'WEB-INF/classes'
    }

    from("$projectDir") {
        include "UCSCExecutables/**"
    }

    from("$projectDir/src/main/webapp") {
        include "getXML.jsp"
        include "getHTML.jsp"
        into 'WEB-INF/classes'
    }
}

task opensourceWar(type: War) {
    archiveFileName = warNameTemp
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    manifest {
        attributes(
                'Built-By': 'u0974549',
                'Implementation-Version': "${gnomex_version} (${formattedDate})"
        )
    }

    // Use UNIX eol convention
    from("${buildDir}/scripts") {
        include "*.sh"
        filter(FixCrLfFilter.class, eol: FixCrLfFilter.CrLf.newInstance("lf"))
        into 'scripts'
    }

    from(buildDir.getAbsolutePath() + "/libs") {
        include "${jarName}"
        include "${clientJarName}"
        into 'WEB-INF/lib'
    }

    from("$projectDir/META-INF") {
        include "context_opensource.xml"
        into 'META-INF'
        rename('context_opensource.xml', 'context.xml')
    }

    from(rescDir) {
        include '*.*'
        into 'WEB-INF/classes'
    }

    from("${rescDir}/hci/gnomex") {
        include 'Dictionaries.xml'
        into 'WEB-INF/classes'
    }

    from("$projectDir") {
        include "UCSCExecutables/**"
    }

    from("$projectDir/src/main/webapp") {
        include "getXML.jsp"
        include "getHTML.jsp"
        into 'WEB-INF/classes'
    }
}

task copyAngular(type: Zip) {
    mustRunAfter(buildWar, buildWarOpensource)
    dependsOn ':gnomex_ng:buildWebpackDevTomcat'
    from zipTree("build/libs/${warNameTemp}")
    from("gnomex_ng/dist") {
        include "**/*"
    }
    exclude 'WEB-INF/lib/hibernate-entitymanager-5.0.10.Final.jar'
    exclude 'WEB-INF/lib/hci_framework.jar'
    exclude 'WEB-INF/lib/javaee-api-7.0.jar'
    exclude 'WEB-INF/lib/javax.mail-1.5.0.jar'
    destinationDir(file("build/libs"))
    archiveName("${warName}")
}

task copyAngularQuick(type: Zip) {
    mustRunAfter(buildWar, buildWarOpensource)
    from zipTree("build/libs/${warNameTemp}")
    from("gnomex_ng/src") {
        include "assets/**"
        include "favicon.ico"
        include "index.html"
    }
    exclude 'WEB-INF/lib/hibernate-entitymanager-5.0.10.Final.jar'
    exclude 'WEB-INF/lib/hci_framework.jar'
    exclude 'WEB-INF/lib/javaee-api-7.0.jar'
    exclude 'WEB-INF/lib/javax.mail-1.5.0.jar'
    destinationDir(file("build/libs"))
    archiveName("${warName}")
}

task copyWar(type: Copy) {
    mustRunAfter(copyAngular, copyAngularQuick)
    from ("build/libs") {
        include "${warName}"
    }
    into "${gnomexServerHome}/${gnomexDeployDir}"
}

task createOpenSourceZip(type: Zip) {
    mustRunAfter(copyAngular, copyAngularQuick)

    archiveFileName = openSourceZipName

    from("$projectDir") {
        include "README_INSTALL.txt"
        filter(FixCrLfFilter.class, eol: FixCrLfFilter.CrLf.newInstance("lf"))
        into openSourceName
    }

    from("$projectDir") {
        include "README_UPGRADE.txt"
        into openSourceName
    }

    from("$projectDir") {
        include "LICENSE.txt"
        into openSourceName
    }

    from("$projectDir/config/tomcat_config") {
        include "*.jar"
        into "${openSourceName}/server/tomcat/lib"
    }

    from("$projectDir/scripts") {
        include "*.*"
        into "${openSourceName}/scripts"
    }

    from(buildDir.getAbsolutePath() + "/libs") {
        include "${warName}"
        include "${clientJarName}"
        into "${openSourceName}/gnomex/build"
    }

    from('gnomex_tomcat_support/build/libs') {
        into "${openSourceName}/gnomex/build"
    }

    from('gnomex_crypt/build/libs') {
        into "${openSourceName}/gnomex/build"
    }

    from("$projectDir/doc") {
        include "*/**"
        into "${openSourceName}/gnomex/doc"
    }

    from("$projectDir/sql") {
        include "*/**"
        into "${openSourceName}/gnomex/sql"
    }
}
